```tefcha
Start
sum = 0, input, i
while i <= input
    i++
    sum += (int)Math.Pow(i, i);
    total = (int)Math.Pow(i, i);
    Console.WriteLine($"{i}^{i} = {total}");
Console.WriteLine(" = " + sum);
End